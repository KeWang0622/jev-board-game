# Copy to .env and fill in to use the live Jev backend.
# Get a key at https://console.typesafe.ai/
TYPESAFE_API_KEY=

# Optional: pin a model version for reproducible experiments (default: jev-latest).
JEV_MODEL=jev-latest
